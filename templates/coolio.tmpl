(function(win, doc) {

	{{ template "sharepoint.js" .}}

	var templates = {
	{{ range $key, $val := .Templates }}
		'{{$key}}': '{{$val}}',
	{{ end}}
	};

	var coolioLayout = {{.JSON}};

	{{ range $js := .Javascript }}
	{{$js}}
	{{ end }}

})(window, document);